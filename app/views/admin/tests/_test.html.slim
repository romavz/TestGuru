tr
  td = test.title
  td = test.level
  td = test.category.title
  td = test.questions.size
  td = link_to t("links.show"), admin_test_path(test)
  td = link_to t("links.edit"), edit_admin_test_path(test)
  td = link_to t("links.destroy"), admin_test_path(test), method: :delete, data: { confirm: "Are you shure?" }
